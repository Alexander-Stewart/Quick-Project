#include <string>
#include <list>

class Camper: public Athlete {
	private:
		string name;

	public: 

		/*
		* a function to get the name of the camper
		* 
		* Returns: a string of the name of the camper
		*/
		string getName() {

		}

		/*
		* a function to set the name of the camper
		*/
		void setName(string newName) {

		}

}